		JLabel label = new JLabel(" Snake Game's Choices ");
		JInternalFrame f = new JInternalFrame(" Snake Game's Choices ");		
		
		JRadioButton ai = new JRadioButton(" Artifitial Intelligence", false);
		JRadioButton hp = new JRadioButton(" Human Player", false);
		JRadioButton exit = new JRadioButton(" Exit", false);
		
		f.add(ai);
		f.add(hp);
		f.add(exit);
		f.setVisible(true);
		f.pack();
		
		board.frame.add(f);
		
		
		label.add(ai);
		label.add(hp);
		label.add(exit);
		label.setVisible(true);
		
		board.addLabel(label);
		
		
		if(ai.isSelected()) return 1;
		if(hp.isSelected()) return 2;
		if(exit.isSelected()) return 0;
		return -1;